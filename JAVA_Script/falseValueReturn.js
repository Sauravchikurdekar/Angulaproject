
//in js true value is print first but in case 3 so many false value so it will print the last one false all the time

const person=null || {name:'amit'};  
console.log(person);

const name='amit' || undefined ||Ankit; 
 console.log(name);

const num=false || undefined || null; 
 console.log(num);

 